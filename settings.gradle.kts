pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

// No additional plugins needed for Gradle 6.7.1

rootProject.name = "FunAutoSend"
include(":app")
